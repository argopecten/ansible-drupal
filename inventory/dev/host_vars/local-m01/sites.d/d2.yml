---
sites:
  d2:
    host: "local-m01"
    name: "{{ vault_local_m01.d2.name }}"
    platform: ap-d11.2
    profile: standard
    modules_enable:
      - syslog
    modules_disable:
      - dblog
    state: present

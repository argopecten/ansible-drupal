---
sites:
  mh:
    host: "mh-host04"
    name: "{{ vault_mh_host04.mh.name }}"
    platform: "drupal11.2"
    profile: "standard"
    admin_user: "admin"
    modules_enable:
      - syslog
    modules_disable:
      - dblog
    cf:
      zone_id: "{{ vault_mh_host04.mh.cf.zone_id | default('dummy') }}"
      api_key: "{{ vault_mh_host04.mh.cf.api_key | default('dummy') }}"
    state: present

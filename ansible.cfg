[defaults]
# Default inventory points to the shared control hosts; override with -i inventory/<client>/hosts.ini
inventory = inventory/control/hosts.ini
host_key_checking = false           # set to true in production
roles_path = roles
playbook_dir = playbooks
ansible_user = ansible              # default SSH user for connecting to managed nodes

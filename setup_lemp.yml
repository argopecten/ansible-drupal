---
- name: Setup LEMP with Ansible
  hosts: localhost
  become: true

  vars:
    php_version: "8.3"
    nginx: true

  roles:
    - common
    - webserver
    - php
    - database
    - composer

- name: Test localhost
  import_playbook: test_localhost.yml
